# Dépendances de développement — inclut le runtime complet
-r requirements.txt

# Serveur MCP (pour le tester en local)
fastmcp==2.9.2

# Tests & qualité
pytest==8.3.4
httpx==0.28.1
ruff==0.8.4
alembic==1.14.1
