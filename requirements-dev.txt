# Dépendances de développement — inclut le runtime complet
-r requirements.txt

# Serveur MCP (pour le tester en local)
fastmcp==2.9.2

# Tests & qualité
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.8.4
alembic==1.14.1

# Vérification de types sur packages/core (cf. mypy.ini)
mypy==1.14.1
types-requests==2.32.0.20241016

# Hooks de pre-commit (lint + types avant chaque commit, cf. .pre-commit-config.yaml)
pre-commit==4.0.1
