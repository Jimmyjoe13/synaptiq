# SynaptiQ — dépendances runtime (API + worker + core + SDK)
# NB : le serveur MCP a ses propres deps (fastmcp) dans apps/mcp/requirements.txt
# Pour l'environnement de dev complet (tests, lint, mcp) : requirements-dev.txt

# --- API FastAPI ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Stockage ---
psycopg2-binary==2.9.10
redis==5.2.1

# --- HTTP / config (worker, embeddings LM Studio, SDK) ---
requests==2.32.3
# NB : >=1.1.0 requis par fastmcp (serveur MCP, requirements-dev). Épingler 1.0.1
# rendait requirements-dev.txt non résoluble.
python-dotenv>=1.1.0,<2

# --- Sécurité (Phase 3 : rate limiting) ---
slowapi==0.1.9
alembic==1.14.1
prometheus-client==0.21.1
