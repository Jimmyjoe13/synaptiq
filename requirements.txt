# SynaptiQ — dépendances runtime de l'API (et du service de migration).
#
# Le socle partagé avec le worker et le relais vit dans requirements-common.txt.
# NB : le serveur MCP a ses propres deps (fastmcp) dans apps/mcp/requirements.txt.
# Pour l'environnement de dev complet (tests, lint, mcp) : requirements-dev.txt

-r requirements-common.txt

# --- API FastAPI ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Sécurité (rate limiting) ---
slowapi==0.1.9

# --- Migrations de schéma (Alembic est la seule autorité du schéma) ---
alembic==1.14.1
